---
import MainLayout from '../layouts/MainLayout.astro';
import Header from '../components/common/Header.astro';
import Container from '../components/ui/Container.astro';
import Button from '../components/common/Button.astro';

const title = "404 - Página No Encontrada | Hikevo Design";
const description = "Lo sentimos, la página que buscas no existe o ha sido movida.";
---

<MainLayout title={title} description={description}>
  <Header />

  <main class="flex-grow flex items-center justify-center min-h-[70vh]">
    <Container>
      <div class="max-w-2xl mx-auto text-center">
        <span class="inline-block px-3 py-1 rounded-full bg-hikevo-gray-light text-hikevo-black text-xs font-bold tracking-widest uppercase mb-6 reveal-smooth">
          Error 404
        </span>

        <h1 class="text-5xl md:text-7xl font-bold text-hikevo-black tracking-tight mb-6 reveal-smooth delay-150">
          Página no encontrada
        </h1>

        <p class="text-lg md:text-xl text-hikevo-charcoal/70 mb-10 leading-relaxed reveal-smooth delay-300">
          Lo sentimos, el enlace que has seguido puede estar roto o la página ha sido movida. 
          Por favor, intenta volver al inicio o contacta con nuestro equipo de soporte.
        </p>

        <div class="flex flex-col sm:flex-row items-center justify-center gap-4 reveal-smooth delay-500">
          <div class="transition-transform duration-500 ease-out hover:scale-105">
            <Button href="/" variant="primary">
              Volver al Inicio
            </Button>
          </div>
          
          <div class="transition-transform duration-500 ease-out hover:scale-105">
            <Button href="/contacto" variant="outline">
              Reportar un Problema
            </Button>
          </div>
        </div>
      </div>
    </Container>
  </main>
</MainLayout>

<style>
  /* Mantenemos el estilo de tracking de la guía de estilo */
  h1 {
    letter-spacing: -0.05em;
  }
</style>